


const SubmitComponent = (props) => {


    return (
        <button type={"submit"} onClick={() => props.submit()} >
            Submit
        </button>
    )




}

export default SubmitComponent;